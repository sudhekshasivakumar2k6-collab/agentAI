# Backend API base URL
# When using the Vite proxy in dev, you don't need to set this (requests to /api are proxied).
# For production, set this to your deployed API URL.
VITE_API_BASE_URL=http://localhost:8000
